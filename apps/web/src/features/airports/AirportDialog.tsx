import { Dialog, DialogContent, DialogHeader, DialogTitle } from '@/components/ui/dialog';

export default function AirportDialog() {
  return (
    <Dialog>
      <DialogContent>
        <DialogHeader>
          <DialogTitle>Airport Dialog</DialogTitle>
        </DialogHeader>
      </DialogContent>
    </Dialog>
  );
}
